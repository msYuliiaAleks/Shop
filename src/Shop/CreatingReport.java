package Shop;

import java.util.Arrays;
/*
public class CreatingReport {
    private Data d = Data.getInstance();
    private Data dr = Data.getInstance();

    public void CreatingReport() {
        dr.dataReport.add(Arrays.asList(("Name,QuantityOfGoodsSold,QuantityOfOrderedGoods,Profit,Outlay").split(",")));
        for (int i = 0; i < d.datalist.size(); ++i) {
            dr.dataReport.add(Arrays.asList((d.datalist.get(i).get(0) + ",0,0,0,0").split(",")));
        }
    }

}
*/